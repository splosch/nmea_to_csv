(function(window){
  var controls = {
    rangeSlider : function(selector){
      item = querySelector(selector);

      rangeSlider.addEventListener('change', function(event) {}, false);
    }("#data_range");
  };

  controls.init();

  return controls;
}(window));
